/**
 * @file dmp_firmware.c
 * @brief MPU-9250 DMP firmware binary placeholder.
 *
 * Run scripts/extract_dmp_firmware.py on the BBB to replace this file
 * with the real 2812-byte MotionApps firmware image.
 *
 * With this stub the IMU will fail to initialise. The robot cannot
 * balance without real firmware. Run the extraction script first.
 */
#include <stdint.h>
#include <stddef.h>

/* Empty placeholder — replace with output of scripts/extract_dmp_firmware.py */
const uint8_t dmp_firmware[]   = { 0x00 };
const size_t  dmp_firmware_size = 0;
